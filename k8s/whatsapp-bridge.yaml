apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whatsapp-auth
  namespace: ia
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 256Mi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: whatsapp-bridge
  namespace: ia
  labels:
    app: whatsapp-bridge
spec:
  replicas: 1
  # Default RollingUpdate briefly runs old+new pods together, and both
  # mount the same ReadWriteOnce PVC for the Baileys auth session files -
  # two processes touching that session concurrently crash-looped both
  # pods until the old one was force-deleted. Recreate avoids that by
  # fully stopping the old pod before starting the new one.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: whatsapp-bridge
  template:
    metadata:
      labels:
        app: whatsapp-bridge
    spec:
      containers:
        - name: whatsapp-bridge
          image: ia/whatsapp-bridge:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3001
          env:
            - name: AGENT_URL
              value: http://agent-backend:3000/message
            - name: ALLOWED_NUMBER
              valueFrom:
                secretKeyRef:
                  name: whatsapp-bridge-secrets
                  key: allowed-number
            - name: PDF_GROUP_ID
              valueFrom:
                secretKeyRef:
                  name: whatsapp-bridge-secrets
                  key: pdf-group-id
          volumeMounts:
            - name: auth
              mountPath: /data/auth
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 384Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
      volumes:
        - name: auth
          persistentVolumeClaim:
            claimName: whatsapp-auth

---
apiVersion: v1
kind: Service
metadata:
  name: whatsapp-bridge
  namespace: ia
spec:
  selector:
    app: whatsapp-bridge
  ports:
    - port: 3001
      targetPort: 3001
